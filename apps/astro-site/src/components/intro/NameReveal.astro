---
// NameReveal.astro - Name reveal animation
---

<div id="scene-names" class="scene names-scene">
  <div id="name-maurice" class="name-text">MAURICE</div>
  <div id="name-calloway" class="name-text">CALLOWAY</div>
</div>

<style>
  .names-scene {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }

  .name-text {
    font-size: 4rem;
    font-weight: 900;
    color: #fff;
    text-shadow:
      0 0 20px #ff1cae,
      0 0 40px #8b4dff,
      0 0 60px #ff1cae;
    letter-spacing: 0.5rem;
    margin: 1rem 0;
    opacity: 0;
    transform: translateY(50px);
    transition: all 0.5s ease-out;
  }

  .name-text.visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* Responsive adjustments */
  @media (max-width: 768px) {
    .name-text {
      font-size: 2rem;
      letter-spacing: 0.2rem;
    }
  }

  @media (max-width: 480px) {
    .name-text {
      font-size: 1.5rem;
      letter-spacing: 0.1rem;
    }
  }
</style>
