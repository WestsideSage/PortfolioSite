---
// DomainExpansionText.astro - Domain Expansion text animation
---

<div id="scene-domain-text" class="scene domain-text-scene">
  <div class="domain-text-container">
    <!-- Japanese text for "Domain Expansion" -->
    <div class="japanese-text">
      <span class="kanji ryoiki">領域</span>
      <span class="kanji tenkai">展開</span>
    </div>

    <!-- English domain name -->
    <div class="domain-name">
      <span class="emerald">EMERALD</span>
      <span class="sanctuary">SANCTUARY</span>
    </div>
  </div>
</div>

<style>
  .domain-text-scene {
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    visibility: hidden;
    transition:
      opacity 0.3s ease,
      visibility 0.3s ease;
  }

  /* Only make scene ready for JavaScript control when intro is initialized */
  .domain-text-scene.intro-ready {
    visibility: visible;
  }

  .domain-text-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 2rem;
    transform: scale(1.2);
  }

  .japanese-text {
    display: flex;
    gap: 1.5rem;
    margin-bottom: 1rem;
  }

  .kanji {
    font-size: 6rem;
    font-weight: 900;
    color: #fff;
    text-shadow:
      0 0 30px #00bfff,
      0 0 60px #0080ff,
      0 0 90px #00bfff;
    font-family: 'Yu Gothic', 'Hiragino Kaku Gothic Pro', 'Meiryo', serif;
    animation: kanjiGlow 2s ease-in-out infinite alternate;
    transform: translateY(20px);
    opacity: 0;
    transition: all 0.8s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .ryoiki {
    animation-delay: 0.2s;
  }

  .tenkai {
    animation-delay: 0.5s;
  }

  .domain-name {
    display: flex;
    gap: 1rem;
    margin-top: 1rem;
  }

  .domain-name span {
    font-size: 3rem;
    font-weight: 900;
    letter-spacing: 0.3rem;
    transform: translateY(30px);
    opacity: 0;
    transition: all 0.8s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .emerald {
    color: #00ff88;
    text-shadow:
      0 0 20px #00ff88,
      0 0 40px #00cc66,
      0 0 60px #00ff88;
    animation-delay: 0.8s;
  }

  .sanctuary {
    color: #88ffaa;
    text-shadow:
      0 0 20px #88ffaa,
      0 0 40px #66cc88,
      0 0 60px #88ffaa;
    animation-delay: 1.1s;
  }

  /* Animation states controlled by JavaScript */
  .domain-text-scene.active .kanji,
  .domain-text-scene.active .domain-name span {
    opacity: 1;
    transform: translateY(0);
  }

  @keyframes kanjiGlow {
    0% {
      text-shadow:
        0 0 30px #00bfff,
        0 0 60px #0080ff,
        0 0 90px #00bfff;
    }
    100% {
      text-shadow:
        0 0 50px #00bfff,
        0 0 80px #0080ff,
        0 0 120px #00bfff;
    }
  }

  /* Responsive adjustments */
  @media (max-width: 768px) {
    .domain-text-container {
      transform: scale(0.8);
      gap: 1.5rem;
    }

    .kanji {
      font-size: 4rem;
    }

    .domain-name span {
      font-size: 2rem;
      letter-spacing: 0.2rem;
    }
  }

  @media (max-width: 480px) {
    .domain-text-container {
      transform: scale(0.6);
      gap: 1rem;
    }

    .kanji {
      font-size: 3rem;
    }

    .domain-name span {
      font-size: 1.5rem;
      letter-spacing: 0.1rem;
    }

    .japanese-text {
      gap: 1rem;
    }

    .domain-name {
      gap: 0.5rem;
    }
  }

  /* Reduced motion support */
  @media (prefers-reduced-motion: reduce) {
    .kanji,
    .domain-name span {
      animation: none !important;
      transition-duration: 0.3s !important;
    }
  }
</style>
