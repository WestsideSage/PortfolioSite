-- Initial schema placeholder
-- Add your database schema here
-- This file is created to prevent migration errors

CREATE TABLE IF NOT EXISTS "placeholder" (
  "id" SERIAL PRIMARY KEY,
  "created_at" TIMESTAMP DEFAULT NOW()
);
